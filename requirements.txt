# AI Mock Interview Agent — pinned dependencies.
# Versions are pinned so a later run cannot silently pull a breaking update.

# ---------------------------------------------------------------------------
# Phase 1 — pure decision core, tests, CLI. Installed and verified on Python 3.9.
# ---------------------------------------------------------------------------
pytest==8.4.2
hypothesis==6.141.1
python-dotenv==1.2.1

# Transitive pins (captured from the working Phase 1 environment).
attrs==26.1.0
exceptiongroup==1.3.1
iniconfig==2.1.0
packaging==26.3
pluggy==1.6.0
sortedcontainers==2.4.0
tomli==2.4.1

# ---------------------------------------------------------------------------
# Phase 2 — voice via gpt-realtime-2 (Azure Foundry) + Astra assessor.
# Verified against live docs (Sep 2026) and pinned from a working environment.
# Requires Python >= 3.9; built and tested here on Python 3.12.
# gpt-realtime on Azure is wired via RealtimeModel.with_azure (GA, no api_version).
# Astra uses the Azure v1 Responses API through the openai SDK (base_url + model).
# ---------------------------------------------------------------------------
livekit-agents==1.8.1
livekit-plugins-openai==1.8.1
openai==2.54.0

# Transitive pins for the voice stack (captured from the working environment).
aiohttp==3.14.3
av==18.1.0
livekit==1.1.18
livekit-api==1.2.1
livekit-blingfire==1.1.0
livekit-local-inference==0.2.7
livekit-protocol==1.1.26
numpy==2.5.3
protobuf==7.36.1
pydantic==2.13.5
pydantic_core==2.46.5
websockets==15.0.1

# ---------------------------------------------------------------------------
# Phase 3 — Tavus avatar (cosmetic, feature-flagged behind AVATAR_ENABLED).
# Verified against live LiveKit docs (Sep 2026): tavus.AvatarSession(pal_id=..,
# face_id=..) reads TAVUS_API_KEY from env; avatar.start() before session.start();
# session runs with RoomOutputOptions(audio_enabled=False) when the avatar is on.
# ---------------------------------------------------------------------------
livekit-plugins-tavus==1.8.1
